import Primeiro from "@/components/basicos/primeiro";

export default function PrimeiraPagina() {
    return(
        <div>
            <Primeiro/>
            <Primeiro/>
            <Primeiro/>
            <Primeiro/>
            <Primeiro/>
        </div>

    ) 
}